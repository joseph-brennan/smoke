#!/bin/bash

#python3 virtualenv wrapper setup
VIRTUALENVWRAPPER_PYTHON=$(which python)
PATH="$PATH:$HOME/.local/bin"
source "$(which virtualenvwrapper.sh)"
workon smoke-editor

pip install -q --user -r requirements-dev.txt
pip install -U -q --user -e .
